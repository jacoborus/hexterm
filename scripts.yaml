scripts:
  test: deno test
  lint: deno lint src
  format:
    - deno fmt src
    - deno fmt README.md
  build:
    cmd:
      - deno test
      - deno run -A src/build_npm.ts
  pre-commit:
    cmd:
      - vr lint
      - vr format
  # publish:
  #   cmd:
  #     - vr build
  #     - cd npm
  #     - npm publish
