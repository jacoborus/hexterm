scripts:
  prepare:
      - deno lint
      - deno fmt
      - deno test
  pre-commit:
    cmd: vr prepare
    gitHook: pre-commit
  npmpublish:
    cmd:
      - vr prepare && deno run -A src/build_npm.ts $1
      - npm publish npm
